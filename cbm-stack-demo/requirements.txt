# requirements.txt

numpy>=1.24.0             # Traitement numérique de base (RMS, FFT, etc.)
scipy>=1.10.0             # FFT, filtres, analyse de signal
argparse~=1.4.0
pytest>=7.0.0             # Tests unitaires
matplotlib>=3.5.0         # Visualisation (si tu veux tracer les signaux)
scikit-learn>=1.5.0       # Algorithmes de classification, clustering (future extension CBM ML)
pandas>=1.3.0             # Manipulation de tableaux complexes, timestamps
scikit-image>=0.19.0      # Analyse d’images (optionnel, utile pour des capteurs visuels/thermiques)
utils~=1.0.2